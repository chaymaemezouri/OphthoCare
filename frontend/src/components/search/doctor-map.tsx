'use client';

import { CabinetMap } from '@/components/medical/cabinet-map';

/** Carte des cabinets / médecins — wrapper sur le composant carte existant. */
export function DoctorMap() {
  return <CabinetMap />;
}
